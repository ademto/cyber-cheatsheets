export const metadata = {
  title: "Hydra",
  category: "Password Cracking",
  slug: "hydra",
};

# Hydra Cheatsheet

Hydra is a fast and flexible password cracking tool.

## Examples

- `hydra -l admin -P passwords.txt ftp://target`
- `hydra -V -f -u -L users.txt -P pass.txt ssh://host`
